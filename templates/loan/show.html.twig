{% extends 'base.html.twig' %}

{% block title %}Loan{% endblock %}

{% block body %}
    <h1>Loan</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ loan.id }}</td>
            </tr>
            <tr>
                <th>NbCoins</th>
                <td>{{ loan.nbCoins }}</td>
            </tr>
            <tr>
                <th>LoanedAt</th>
                <td>{{ loan.loanedAt ? loan.loanedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_loan_index') }}">back to list</a>

    <a href="{{ path('app_loan_edit', {'id': loan.id}) }}">edit</a>

    {{ include('loan/_delete_form.html.twig') }}
{% endblock %}
